# Required for AI reasoning (set in your hosting provider — never commit real keys)
OPENAI_API_KEY=

# Optional: override model (default: gpt-4.1)
CLINICAL_MODEL=gpt-4.1

# Optional: vision model for image/PDF-adjacent OCR (default: gpt-4.1-mini)
CLINICAL_VISION_MODEL=gpt-4.1-mini

# Optional: protect POST /api/knowledge/reindex in production
# ADMIN_SECRET=

# Future: vector DB for guideline RAG
# PINECONE_API_KEY=
# PINECONE_INDEX=
